- content_for :row
  .order-index-page
    - if @orders.blank?
      h3.text-center 您还没有任何订单。
    - else
      .order-cards-list
        = render partial: 'users/orders/order', collection: @orders, cache: true
