- content_for :row
  .order-show-page
    .col-md-6.col-md-offset-3
      .panel.panel-default
        .panel-body
          - case @order.state
          - when 'requested', 'contracted', 'payment', 'completed'
            = render "users/orders/show/#{@order.state}", order: @order
      .panel.panel-default
        .panel-body
          span.fa-icon
            => fa_icon('envelope')
          strong 维修内容
          p = @order.content

          span.fa-icon
            => fa_icon('phone')
          strong 联系方式
          p
            = current_user.name
            br
            = current_user.readable_phone_number
            br
            = @order.address.full_content

          span.fa-icon
            => fa_icon('clock-o')
          strong 预约时间
          - if (@order.requested? || @order.contracted?) && @order.arrives_at < Time.now
            p
              = l @order.arrives_at
              strong （时间已过，请您自行联系师傅预约）
          - else
            p = l @order.arrives_at
