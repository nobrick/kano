= render_admin_account_breadcrumb(@user, "订单信息")

= render partial: 'admin/shared/dashboard/index', locals: { resources: @orders, dashboard: dashboard, instance: @search, filter_path_query: { user_id: @user.id }}
