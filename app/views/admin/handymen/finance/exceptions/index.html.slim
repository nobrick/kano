= render_admin_account_breadcrumb(@handyman, "财务信息")

= render_admin_handyman_finance_tabs(@handyman)

= render partial: 'admin/shared/dashboard/index', locals: { resources: @failed_withdrawals, dashboard: dashboard, instance: nil }

